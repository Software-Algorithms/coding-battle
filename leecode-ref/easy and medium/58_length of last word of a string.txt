public class Solution {
    public int lengthOfLastWord(String s) {
        int count = 0;
        char[] charStr = s.trim().toCharArray();
        for(int i = charStr.length-1;i>=0;i--){      	
        	if(charStr[i]==' ') return count;
        	count++;
        }
        return count;
    }
}